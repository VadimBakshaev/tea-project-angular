import { TextCroppingPipe } from './text-cropping-pipe';

describe('TextCroppingPipe', () => {
  it('create an instance', () => {
    const pipe = new TextCroppingPipe();
    expect(pipe).toBeTruthy();
  });
});
